name: Notify Snapshot

on:
  workflow_dispatch:

jobs:
  build:
    uses: Crazy-Crew/.github/.github/workflows/notify.yml@main

    with:
      status: "beta"
      branch: main

    secrets:
      webhook: ${{ secrets.CA_WEBHOOK }}